# مستندات فنی — قبله‌نما و اوقات شرعی

**طراح و توسعه‌دهنده:** امید شاهین مهر — Omidshahinmehr@gmail.com

---

## ۱. معرفی کلی

اپلیکیشن اندرویدی بومی (Kotlin + Jetpack Compose) با قابلیت‌های زیر:

1. قبله‌نمای گرافیکی — محاسبه‌ی محلی (بدون اینترنت) + عقربه‌ی زنده با سنسور قطب‌نمای گوشی
2. اوقات شرعی روزانه بر پایه‌ی روش «مؤسسه ژئوفیزیک دانشگاه تهران»، با پشتیبان آفلاین کامل
3. جدول اوقات شرعی کل ماه، قابل مرور به عقب/جلو
4. زنگ (آلارم) هوشمند برای هر اذان با صدای دلخواه کاربر
5. یادآوری سبک چند دقیقه پیش از هر اذان (جدا از خود آلارم)
6. مدیریت شهر: موقعیت فعلی، انتخاب از نقشه، وارد کردن مختصات دستی، و جست‌وجوی نام شهر (محلی + آنلاین)
7. ویجت صفحه‌ی اصلی نمایش‌دهنده‌ی اوقات شرعی + تاریخ شمسی و قمری
8. حالت روشن/تیره با ذخیره‌ی انتخاب کاربر
9. صفحه‌ی «درباره» با اطلاعات طراح

حداقل نسخه‌ی پشتیبانی‌شده: **Android 5.0 (API 21)** — برای پشتیبانی از گوشی‌های قدیمی‌تر و صفحه‌نمایش‌های کوچک.

---

## ۲. معماری

الگوی **MVVM** با یک `AndroidViewModel` مرکزی (`QiblaViewModel`) که state را از طریق `StateFlow` به Composeها می‌دهد. صفحات با `Navigation Compose` به هم وصل‌اند (`ui/Navigation.kt`).

```
com.qibla.prayertimes/
  model/       City.kt                         مدل داده‌ی شهر + فهرست شهرهای پیش‌فرض
  data/        QiblaMath.kt                     محاسبه‌ی جهت و فاصله تا کعبه (محلی)
               PrayerTimesRepository.kt         اوقات شرعی امروز: آنلاین (Aladhan) + بازگشت به آفلاین
               OfflinePrayerCalculator.kt       محاسبه‌ی نجومی محلی اوقات شرعی (بدون اینترنت)
               MonthlyPrayerTimesRepository.kt  اوقات شرعی کل ماه: آنلاین + آفلاین
               GeocodingSearch.kt               جست‌وجوی آنلاین نام شهر (Nominatim/OpenStreetMap)
               CityStore.kt                     ذخیره‌سازی شهر انتخابی/سفارشی (SharedPreferences)
               LocationHelper.kt                موقعیت‌یابی بومی (LocationManager + Geocoder)
               WidgetDataStore.kt               کش مشترک بین ویجت، آلارم و صفحه‌ی اصلی
  alarm/       AlarmPrefs.kt                    تنظیمات هر اذان (روشن/خاموش، صدا، دقیقه‌ی یادآوری)
               AlarmScheduler.kt                زمان‌بندی AlarmManager (آلارم + یادآوری) بر اساس سطح API
               AdhanAlarmReceiver.kt            دریافت broadcast زمان اذان/یادآوری
               AdhanPlaybackService.kt          پخش صدای اذان + نوتیفیکیشن foreground
               ReminderNotifier.kt              نمایش اعلان یادآوری سبک (بدون سرویس/صدای طولانی)
               StopAdhanReceiver.kt             قطع صدا از دکمه‌ی نوتیفیکیشن
               BootReceiver.kt                  زمان‌بندی مجدد پس از ری‌استارت گوشی
  sensor/      CompassSensor.kt                 خواندن جهت گوشی از سنسور (rotation vector / accel+mag)
  work/        PrayerTimesWorker.kt             بروزرسانی پس‌زمینه‌ی روزانه (WorkManager)
  widget/      QiblaWidget.kt                   محتوای ویجت (Jetpack Glance)
               QiblaWidgetReceiver.kt           AppWidgetProvider
               QiblaWidgetUpdater.kt            درخواست بازترسیم ویجت
  util/        JalaliCalendar.kt                تبدیل میلادی به شمسی (الگوریتم jalaali دقیق)
               HijriCalendar.kt                 تبدیل میلادی به قمری تقریبی (تقویم جدولی، فقط حالت آفلاین)
  viewmodel/   QiblaViewModel.kt                هماهنگ‌کننده‌ی همه‌ی موارد بالا برای صفحه‌ی اصلی
  ui/theme/    Color.kt                         سامانه‌ی رنگ روشن/تیره (توکن‌های theme-aware)
               Theme.kt                         اتصال به Material3 ColorScheme
  ui/          QiblaScreen.kt                   صفحه‌ی اصلی (قبله‌نما + اوقات امروز)
               CompassDial.kt                   ترسیم قبله‌نمای اسطرلابی با Canvas
               PrayerTimesCard.kt                کارت اوقات شرعی امروز
               CityPickerSheet.kt               انتخاب/جست‌وجوی شهر
               MapPickerDialog.kt                افزودن شهر از نقشه/مختصات دستی
               AlarmSettingsScreen.kt            تنظیمات زنگ و یادآوری اذان
               MonthlyTimesScreen.kt             جدول اوقات شرعی ماهانه
               AboutScreen.kt                    درباره‌ی برنامه
               Navigation.kt                    مسیرهای بین صفحات
  MainActivity.kt
```

---

## ۳. الگوریتم‌های محاسباتی

### ۳.۱ جهت قبله

فرمول Great-Circle Bearing استاندارد، نسبت به مختصات کعبه (`21.4225°N, 39.8262°E`):

```
Δλ = λ_kaaba − λ_you
y = sin(Δλ) · cos(φ_kaaba)
x = cos(φ_you)·sin(φ_kaaba) − sin(φ_you)·cos(φ_kaaba)·cos(Δλ)
bearing = atan2(y, x)  →  نرمال‌شده به بازه‌ی ۰ تا ۳۶۰ درجه
```

فاصله هم با فرمول Haversine محاسبه می‌شود. هر دو کاملاً محلی هستند (`data/QiblaMath.kt`).

**قبله‌نمای زنده:** اگر گوشی سنسور جهت داشته باشد (`sensor/CompassSensor.kt` — اول `TYPE_ROTATION_VECTOR`، در نبود آن ترکیب `TYPE_ACCELEROMETER` + `TYPE_MAGNETIC_FIELD`)، عقربه به‌جای نمایش ثابتِ «از شمال»، به‌صورت زنده با فرمول `زاویه‌ی نمایش = قبله − جهت گوشی` می‌چرخد؛ یعنی وقتی گوشی رو به قبله باشد، فلش دقیقاً رو به بالا می‌ایستد. یک فیلتر میان‌گذر (low-pass) با کوتاه‌ترین مسیر برای عبور از مرز ۰/۳۶۰ درجه، از لرزش و چرخش نادرست تصویر جلوگیری می‌کند. اگر سنسوری در دسترس نباشد، به حالت ثابت (نمایش نسبت به شمال جغرافیایی) بازمی‌گردد.

### ۳.۲ اوقات شرعی

از سرویس عمومی **Aladhan API** استفاده می‌شود:
```
GET https://api.aladhan.com/v1/timings/{dd-MM-yyyy}?latitude=..&longitude=..&method=7
GET https://api.aladhan.com/v1/calendar/{year}/{month}?latitude=..&longitude=..&method=7   (جدول ماهانه)
```
`method=7` دقیقاً معادل «مؤسسه ژئوفیزیک دانشگاه تهران» است (زاویه‌ی فجر ۱۷.۷ درجه، زاویه‌ی عشاء ۱۴ درجه).

**حالت آفلاین:** اگر شبکه در دسترس نباشد یا درخواست با خطا مواجه شود، `PrayerTimesRepository` و `MonthlyPrayerTimesRepository` به‌طور خودکار به `OfflinePrayerCalculator` سوییچ می‌کنند — پیاده‌سازی محلی همان الگوریتم نجومی استاندارد (موقعیت خورشید، معادله‌ی زمان، زاویه‌ی ساعتی) با پارامترهای یکسان با روش دانشگاه تهران. نتیجه با پرچم `isOffline=true` علامت‌گذاری می‌شود تا رابط کاربری برچسب «آفلاین · تقریبی» را نشان دهد. منطقه‌ی زمانی از `TimeZone.getDefault()` گرفته می‌شود. تاریخ قمری هم در این حالت با یک تقویم هجری قمری جدولی (محاسباتی، نه رصد هلال) در `util/HijriCalendar.kt` تخمین زده می‌شود.

### ۳.۳ تقویم شمسی (جلالی)

الگوریتم مرجع **jalaali-js** (مبتنی بر Julian Day Number و جدول شکست ۳۳ ساله‌ی کبیسه) به کاتلین پورت شده (`util/JalaliCalendar.kt`)، در برابر تاریخ‌های مرجع (۱ فروردین ۱۴۰۳ = ۲۰ مارس ۲۰۲۴) صحت‌سنجی شده است.

### ۳.۴ تقویم قمری تقریبی (فقط حالت آفلاین)

تقویم هجری قمریِ جدولی/محاسباتی (epoch مدنی، چرخه‌ی ۳۰ ساله با ۱۱ سال کبیسه) در `util/HijriCalendar.kt`، در برابر تاریخ مرجع شناخته‌شده (۱ محرم ۱۴۴۴ = ۳۰ ژوئیه ۲۰۲۲) صحت‌سنجی شده و معمولاً حداکثر یک روز با تاریخ رسمی رصدی فاصله دارد.

---

## ۴. سامانه‌ی آلارم و یادآوری اذان

### روند کار
1. هر بار که `QiblaViewModel` اوقات شرعی جدید می‌گیرد، آن‌ها را در `WidgetDataStore` کش می‌کند و `AlarmScheduler.scheduleToday()` را صدا می‌زند.
2. `AlarmScheduler` برای هر اذانی که کاربر روشن کرده یک آلارم دقیق، و برای هر اذانی که «یادآوری» آن روشن است یک آلارم جداگانه‌ی زودتر (با کد درخواست متفاوت: `requestCode + 500`) تنظیم می‌کند.
3. با فرارسیدن زمان آلارم اصلی، `AdhanAlarmReceiver` یک `Foreground Service` (`AdhanPlaybackService`) راه‌اندازی می‌کند که صدای انتخابی کاربر را پخش و نوتیفیکیشن با دکمه‌ی «قطع صدا» نمایش می‌دهد.
4. با فرارسیدن زمان یادآوری، همان `AdhanAlarmReceiver` مستقیماً `ReminderNotifier` را صدا می‌زند که یک اعلان سبک و معمولی (بدون سرویس، بدون پخش طولانی صدا) نشان می‌دهد.
5. `BootReceiver` پس از روشن‌شدن مجدد گوشی، از طریق `PrayerTimesWorker` زمان‌بندی را از نو انجام می‌دهد (چون هشدارهای `AlarmManager` با ری‌استارت گوشی پاک می‌شوند).

### تطبیق با نسخه‌های مختلف اندروید
| سطح API | رفتار |
|---|---|
| ۳۱+ (Android 12+) | بررسی مجوز `canScheduleExactAlarms()`؛ در صورت نبود مجوز، آلارم غیردقیق و بنر راهنما برای باز کردن تنظیمات |
| ۲۳–۳۰ | `setExactAndAllowWhileIdle` (کار می‌کند حتی در Doze) |
| ۲۱–۲۲ | `setExact` (متد جدیدتر در این نسخه‌ها وجود ندارد) |

### انتخاب صدا
کاربر از دو مسیر صدا انتخاب می‌کند (به دلایل حجم برنامه و حق نشر، صدای اذان از پیش تعبیه نشده):
- **زنگ‌های دستگاه**: از طریق `RingtoneManager.ACTION_RINGTONE_PICKER`
- **فایل صوتی دلخواه**: از طریق Storage Access Framework (`ACTION_OPEN_DOCUMENT`) با مجوز پایدار (`takePersistableUriPermission`)

---

## ۵. ویجت صفحه‌ی اصلی

با **Jetpack Glance** ساخته شده. محتوای ویجت از `WidgetDataStore` (کش SharedPreferences مشترک) خوانده می‌شود، نه مستقیم از شبکه، تا حتی بدون اینترنت هم آخرین داده‌ی معتبر (یا محاسبه‌ی آفلاین) نمایش داده شود. `PrayerTimesWorker` هر ۲۴ ساعت یک‌بار به‌صورت پس‌زمینه اجرا می‌شود تا ویجت و آلارم‌ها حتی بدون باز کردن اپ هم به‌روز بمانند.

---

## ۶. مدیریت شهر و جست‌وجو

چهار راه برای تعیین موقعیت وجود دارد، همه بدون نیاز به اینترنت به‌جز مورد آخر:
1. **موقعیت فعلی**: `LocationManager` بومی (بدون Google Play Services)؛ نام‌گذاری معکوس با `Geocoder` تلاش می‌شود ولی در نبود اینترنت با نام عمومی «موقعیت فعلی» جایگزین می‌شود.
2. **انتخاب از نقشه**: ضربه روی یک شبکه‌ی مختصات (graticule) که همیشه به‌صورت محلی رسم می‌شود؛ اگر اینترنت باشد تصویر واقعی نقشه هم روی آن بارگذاری می‌شود.
3. **وارد کردن مختصات دستی**: عرض/طول جغرافیایی به‌صورت مستقیم، با اعتبارسنجی محدوده.
4. **جست‌وجوی نام شهر**: ابتدا فیلتر آنی و محلی روی فهرست شهرهای پیش‌فرض و شهرهای کاربر (`CityPickerSheet`)؛ اگر نتیجه‌ی کافی نبود و اینترنت در دسترس بود، جست‌وجوی آنلاین با API رایگان **Nominatim/OpenStreetMap** انجام می‌شود (`data/GeocodingSearch.kt`) که نیاز به کلید API ندارد و طبق سیاست استفاده‌ی آن سرویس با یک هدر `User-Agent` مشخص فراخوانی می‌شود.

---

## ۷. حالت روشن/تیره

سامانه‌ی رنگ در `ui/theme/Color.kt` با یک شیء `ThemeState` (که مقدارش با Compose state نگه داشته می‌شود) پیاده شده است. هر توکن رنگ (`AmberText`, `NightMid`, `Brass` و…) به‌جای مقدار ثابت، یک property با `get()` است که بسته به `ThemeState.isDark` بین پالت تیره (اصلی) و پالت روشن (کرم گرم/طلایی) سوییچ می‌کند؛ به همین دلیل نیازی به تغییر کدهای هر صفحه نبود — همه‌ی صفحات به‌طور خودکار با تغییر حالت، رنگشان به‌روز می‌شود. انتخاب کاربر در `SharedPreferences` (`ThemePrefs`) ذخیره و در اجرای بعدی بازیابی می‌شود.

---

## ۸. سازگاری با گوشی‌های قدیمی و صفحه‌نمایش کوچک

- `minSdk = 21` (Android 5.0 Lollipop به بالا).
- تمام فراخوانی‌های API-محور با بررسی `Build.VERSION.SDK_INT` شاخه‌بندی شده‌اند.
- قبله‌نما (`CompassDial`) اندازه‌ی خود را پارامتری می‌گیرد؛ در `QiblaScreen` با `BoxWithConstraints` عرض واقعی صفحه سنجیده و در صفحه‌های کوچک‌تر از ۳۶۰dp با مقیاس کوچک‌تر رسم می‌شود.
- کل صفحه در یک `Column` با قابلیت اسکرول عمودی است.

---

## ۹. مجوزهای اندروید و دلیل هرکدام

| مجوز | دلیل |
|---|---|
| `INTERNET` | اوقات شرعی، جدول ماهانه، تصویر نقشه، جست‌وجوی آنلاین شهر |
| `ACCESS_FINE_LOCATION` / `ACCESS_COARSE_LOCATION` | دکمه‌ی «موقعیت فعلی من» |
| `SCHEDULE_EXACT_ALARM` / `USE_EXACT_ALARM` | زمان‌بندی دقیق اذان (اندروید ۱۲+) |
| `POST_NOTIFICATIONS` | نمایش نوتیفیکیشن اذان/یادآوری (اندروید ۱۳+) |
| `RECEIVE_BOOT_COMPLETED` | زمان‌بندی مجدد آلارم‌ها پس از ری‌استارت |
| `FOREGROUND_SERVICE` / `FOREGROUND_SERVICE_MEDIA_PLAYBACK` | پخش صدای اذان به‌عنوان سرویس foreground |
| `WAKE_LOCK` | اطمینان از پخش صدا حتی اگر صفحه خاموش باشد |

سنسورهای قطب‌نما (rotation vector / accelerometer / magnetometer) نیازی به مجوز runtime ندارند.

---

## ۱۰. محدودیت‌های شناخته‌شده

- در حالت آفلاین، اوقات شرعی و تاریخ قمری با محاسبه‌ی محلی (نه سرویس رسمی) به‌دست می‌آیند و با برچسب «تقریبی» نمایش داده می‌شوند.
- فایل صوتی اذان در برنامه تعبیه نشده؛ کاربر باید صدا را از فایل‌های گوشی یا زنگ‌های سیستمی انتخاب کند.
- بدون مجوز «زمان‌بندی دقیق» در اندروید ۱۲+، آلارم ممکن است چند دقیقه تأخیر داشته باشد.
- بهینه‌سازی باتری برخی گوشی‌ها ممکن است اجرای `WorkManager` را به تأخیر بیندازد.
- جست‌وجوی آنلاین شهر و تصویر نقشه بدون اینترنت کار نمی‌کنند (بقیه‌ی روش‌های افزودن شهر آفلاین‌اند).
- قبله‌نمای زنده به کیفیت سنسور مغناطیسی گوشی وابسته است؛ نزدیکی به فلزات یا دستگاه‌های الکترونیکی می‌تواند دقتش را کم کند.

---

## ۱۱. ساخت پروژه

```
Android Studio → Open → پوشه‌ی QiblaApp
منتظر Sync خودکار Gradle بمانید → Run ▶
```

---

## ۱۰. بازطراحی نسخه‌ی جدید (تغییرات اخیر)

### ساختار ناوبری جدید
`home` → `menu` (منو با دسترسی به: انتخاب شهر، زنگ اذان، جدول ماهانه، زبان، درباره) → هرکدام مسیر جدا. `QiblaViewModel` اکنون Activity-scoped است (نه per-destination) تا انتخاب شهر بین صفحه‌ی اصلی و صفحه‌ی انتخاب شهر همگام بماند.

### انتخاب شهر (۴ روش)
`CityPickerScreen` (تمام‌صفحه، جایگزین bottom sheet قبلی):
1. انتخاب از لیست (پیش‌فرض + شهرهای من، با جست‌وجوی محلی/آنلاین)
2. موقعیت فعلی → پرامپت نام‌گذاری (`NamePromptDialog`) → ذخیره
3. نقشه → `MapPickerScreen` تمام‌صفحه با دکمه‌ی «انتخاب» پایین صفحه → برگشت به `CityPickerScreen` (هندشیک نتیجه از طریق یک `mutableStateOf` مشترک در سطح `QiblaNavHost`, نه SavedStateHandle) → پرامپت نام‌گذاری
4. وارد کردن دستی (`ManualEntryDialog`): نام + عرض/طول جغرافیایی

### صفحه‌ی اصلی (بازطراحی کامل)
نوار بالا: نام شهر + آیکون مکان (باز کردن انتخاب شهر) / دکمه‌ی منو. سپس متن صلوات (فونت جایگزین `FontFamily.Serif` — فونت «انتظار» واقعی در دسترس نبود؛ برای افزودنش فایل را در `res/font/entezar.ttf` قرار دهید و `salawatFontFamily` در `QiblaScreen.kt` را به‌روزرسانی کنید). سپس سه تاریخ هم‌زمان (میلادی، شمسی، قمری). سپس ۶ وقت شرعی (صبح، طلوع، ظهر، غروب، مغرب، نیمه‌شب). در پایان، قبله‌نما.

### قبله‌نما
وسط قبله‌نما اکنون وردمارک «LBo» را با فونت باندل‌شده (`res/font/logo_serif_bold.ttf`) نمایش می‌دهد. همچنین باگ چرخش ناگهانی عقربه هنگام عبور از مرز ۰/۳۶۰ درجه با یک ردیاب زاویه‌ی پیوسته (unwrapped) رفع شد.

### جدول ماهانه
دو حالته: میلادی (پیش‌فرض) و شمسی. برای حالت شمسی، `JalaliCalendar.toGregorian()` (تابع معکوس جدید) هر روز شمسی را به میلادی تبدیل کرده و از حداکثر دو ماه میلادی (چون یک ماه شمسی معمولاً بین دو ماه میلادی می‌افتد) داده می‌گیرد.

### ویجت
یک `TextClock` واقعی (از طریق `AndroidRemoteViews`، چون خود Glance ساعت زنده ندارد) + شمارش معکوس تا نمازِ بعدی (از میان پنج نماز اصلی) + اوقات شرعی به‌جز عصر و امساک. یک `WidgetRefreshWorker` هر ۱۵ دقیقه (کمترین بازه‌ی مجاز WorkManager) فقط ویجت را از کش بازترسیم می‌کند تا شمارش معکوس به‌روز بماند، بدون تماس شبکه‌ای اضافه.

### زبان برنامه
`LocalePrefs` + بازنویسی `attachBaseContext` در `MainActivity` امکان انتخاب دستی زبان (فارسی/انگلیسی/عربی/پیش‌فرض سیستم) را از داخل منو فراهم می‌کند، مستقل از زبان سیستم.

### رفع باگ
کرش صفحه‌ی «درباره» به‌خاطر تلاش برای بارگذاری آیکون adaptive (`mipmap-anydpi-v26`) با `painterResource` بود؛ اکنون یک تصویر raster اختصاصی (`drawable-nodpi/app_logo.png`) برای این منظور استفاده می‌شود.

---

## ۱۱. رفع باگ بیلد و به‌روزرسانی‌های اخیر

- **باگ کامپایل واقعی رفع شد:** `MonthlyTimesScreen.kt` از مسیر اشتباه `androidx.compose.ui.LocalLayoutDirection` ایمپورت می‌کرد؛ مسیر درست `androidx.compose.ui.platform.LocalLayoutDirection` است.
- **فونت «انتظار» واقعی اضافه شد:** فایل در `res/font/entezar.ttf` قرار گرفت و برای متن صلوات در صفحه‌ی اصلی استفاده می‌شود.
- **انتخاب از نقشه اکنون یک نقشه‌ی واقعی و تعاملی OpenStreetMap است:** به‌جای شبکه‌ی مختصات آفلاین + تصویر ثابت قبلی، از یک `WebView` با کتابخانه‌ی Leaflet.js و کاشی‌های OpenStreetMap استفاده می‌شود (`assets/map_picker.html`). چون این مسیر ذاتاً به اینترنت نیاز دارد (کشیدن نقشه + کاشی‌ها)، راه آفلاین معادل همان «وارد کردن مختصات دستی» در صفحه‌ی انتخاب شهر است.
